# Data Munging with Perl (2ed) — Update TODO

Findings from a full read-through of `Data Munging with Perl (2ed).pdf` (234 pages), July 2026. Updated after the `dmp` git repo was added to the project.

**Source of truth note:** the `dmp/` folder is the actual Git repo for the book (`chapters/*.md`, built to epub/pdf via the `Makefile`). Confirmed via PDF metadata (`Producer: Skia/PDF ... Google Docs Renderer`) that the standalone PDF is exported from Google Docs, not from this repo — so, as suspected, edits have been happening in both places independently. A full paragraph-and-word-level diff of every chapter (PDF text vs. `chapters/*.md`, 2026-08-05) found the drift is **not one-directional** — see "Sync PDF ↔ Markdown" below before treating either source as canonical wholesale.

**LeanPub note (2026-08-05):** updates reach the LeanPub WIP edition via manual upload — no GitHub sync is configured, so a built EPUB/PDF from this repo has to be produced and uploaded by hand each time.

**Completion estimate (2026-08-06):** for the LeanPub "percentage complete" field, set to **65%**. My own read was 75–80% — every chapter is structurally complete and readable, and the worst staleness (dead XML chapter, broken weather example, ancient parser tooling) is fixed — but Dave's going with the more pessimistic figure given what's still open below: real rewrites still needed for Ch5 (Unicode) and Ch6 (dates), the Appendix A drift found today, and the smaller polish items (copyright wording, artwork, Appendix B). Worth revisiting this number each time a chapter from the "Outdated code / modules" or "New topics" lists below gets closed out.

## 2026-08-05 pre-release sprint — done

- [x] **Chapter 11 rewritten for `Regexp::Grammars`.** All three examples (English sentences, INI file, CD data file) now use `Regexp::Grammars` throughout, replacing the leftover `Parse::RecDescent` sections. New material covers array-capturing subrules (`<[Name]>`), the separated-list syntax (`<[Name]>+ %sep`), the `<debug:...>` directive (replacing `$::RD_TRACE`/`$::RD_HINT`), and a rewritten "Other features" list (tokens vs. rules, `<require:>`, `<error:>`/`<fatal:>`, `<objrule:>`/`<objtoken:>`, `<grammar:>`/`<extends:>`). The CD example now also uses `trim()` from `builtin` to strip fixed-width padding, which the old version silently left in.
- [x] **`new-code/c11/*` promoted into `code-examples/c11/`**, replacing the old Parse::RecDescent files. Fixed a genuine bug in the process: the CD sample data has 6 records but the footer said "7 Records" in `cds.pl`/`cds_json.pl`, and was missing from `cds.txt` entirely — all three now correctly say "6 Records".
- [x] **Fixed the EPUB build.** `make epub` wasn't resolving `../images/*.png` paths from the repo root, so every image silently failed to embed. Added `--resource-path=.:chapters` to the Makefile's pandoc invocation — verified a clean build with zero warnings and every image embedded.
- [x] **Copyright page now has a self-dating WIP version line.** Added a Copyright section to `chapters/front-matter.md` with `Work in progress version: __BUILD_DATE__`. The Makefile generates `build/front-matter.md` with that token substituted for today's date (`date +%Y-%m-%d`) on every build, so you never have to remember to hand-edit it. `build/` is gitignored.
- [ ] **Needs your review:** the actual copyright wording (publisher, rights language, years) — flagged with a `TODO(Dave)` comment right above it in `front-matter.md` rather than guessing.
- [x] ~~`make pdf` is untested~~ — **confirmed working.** Switched `pdf` from Calibre's `ebook-convert` to `pandoc --pdf-engine=weasyprint` straight from the markdown (lighter dependency, easier for CI later, avoids the LaTeX escaping issue mentioned below). Added `print.css` for page size/margins/running page numbers/forced chapter breaks. Dropped `.mobi`/`kindlegen` support entirely — Amazon deprecated kindlegen years ago.
- [x] ~~Image references broke under WeasyPrint~~ — WeasyPrint resolves relative image paths against the current directory rather than honoring pandoc's `--resource-path` the way the EPUB builder does. All 17 `../images/...` references (chapters 1, 2, 3, 8, 10, 11) changed to root-relative `images/...`, `--resource-path` simplified to `.`. Both `make epub` and `make pdf` confirmed working.

## Sync PDF (Google Docs) ↔ Markdown — findings from full diff

Compared every chapter's PDF text against its `.md` file. Most chapters matched almost exactly (front matter, ch2, ch5, ch6, ch7, ch8, ch9, ch10, ch12, appendix-a, appendix-b — only trivial table/formatting artifacts, no real content drift). Three chapters have genuine divergence:

- [x] ~~Chapter 11 is the big one.~~ **Done (2026-08-05)** — fully rewritten for `Regexp::Grammars` throughout, using the ready-made code from `new-code/c11/`. See sprint summary above.
- [x] ~~Chapter 1 is missing two things the PDF has.~~ **Done (2026-08-05)** — ported the CD-file editorial aside and the "YAML and JSON... largely taken over from XML" sentence from the PDF into `chapters/chapter01.md`.
- [ ] **Chapter 3 has the opposite problem** — four short "note-to-self"-style asides exist only in `chapters/chapter03.md` (each bounded by a `---` rule) and never made it into Google Docs: a note on `Memoize`/`List::UtilsBy` as modern alternatives to the sort techniques discussed; a note recommending `DBIx::Class` after the DBI section (references "my article Database Access with DBIx::Class"); a note on `Devel::NYTProf` for profiling; and a note on `use feature`, `use VERSION`, and the `-E` flag after the command-line scripts section. These read like draft jottings (informal tone, one typo: "Orciah Manoeuvre" for "Orcish Manoeuvre") rather than finished prose. **Decided: no action needed** — the markdown already has the best version of this content; nothing to port anywhere.

## Ready-to-use assets found in the repo (2026-08-05 untracked-files audit)

The `dmp` folder had 28 untracked files. Most were noise, but a few are drafted work ready to fold in:

- [x] ~~`new-code/c11/*`~~ — promoted into `code-examples/c11/` and wired into the rewritten Chapter 11 (2026-08-05).
- [x] ~~Two competing weather-example drafts for Chapter 9~~ — **resolved (2026-08-06):** neither used. Both deleted; replaced with a live Open-Meteo API example in Chapter 10's new JSON section, cross-linked from Chapter 9. See the 2026-08-06 JSON/YAML sprint entry below.
- [x] ~~`code-examples/c3/data_printer.pl` + `cd.txt`~~ — not orphaned, just uncommitted: companion code for the `Data::Printer` section already written into `chapter03.md` (lines 603–611). Just needs to be committed.
- [x] ~~14 `chapters/*.docx` files~~ — **deleted (2026-08-05)**. Spot-checked `chapter11.docx` word-for-word against `chapter11.md`: 19 words different out of 4,500. They were just Word exports of the markdown, not a separate source of content.
- Noise, not content: `index.html` (your personal site's homepage, davecross.co.uk — copied in by accident) and `.vscode/` (VS Code's Perl language-server cache). Neither was deleted; worth binning `index.html` and adding `.vscode/` to `.gitignore` when convenient.
- Kept as-is, not book content but harmless: `add-issues`/`import-issues`/`create-labels` (your tooling for pushing the TODO backlog below into GitHub Issues/Projects), `dmp-cover.jpg`/`dmp2-cover.jpg`/`dmp2-cover.webp` (1st + 2nd edition cover art — worth committing properly at some point).

## Structural issues

- [x] ~~Chapter 4 (pattern matching) is missing.~~ **Resolved** — `chapters/chapter04.md` exists in the `dmp` repo, complete (~1050 lines, substr/index/case functions through regular expressions, with summary). It's wired into `chapters.txt` and the build. The standalone PDF was just out of sync with the repo — last repo commit touching chapter 4 is 2024-06-03, which is also the most recent commit in the whole repo, so the PDF was likely exported before that, or via a separate path that dropped it.
- [x] ~~"Part IV" is referenced but doesn't exist.~~ **Resolved (2026-09-01):** the preface said "PART IV concludes our tour..." but Chapter 12 just followed Part III directly, with no Part IV divider in the actual document. Turned out Part III also still had its original "beast/heroes" fantasy-narrative text (see `orig/dmp.txt`) even though Parts I and II had already been rewritten in plain prose — fixed both: replaced Part III's narrative text in `chapter08.md` with plain prose matching Parts I/II's style, added an actual "Part IV - The big picture" heading to `chapter12.md`, and restored the front matter's original "PART IV concludes our tour..." sentence as a proper link (the sentence itself was fine, word-for-word from the 2001 original — the bug was the missing heading it pointed at, not the sentence).
- [x] ~~Part III's own heading is misleading.~~ **Resolved (2026-09-14):** "Part III - Simple data parsing" contradicted both Chapter 8's title and its own intro paragraph. Renamed to "Part III - Data parsing" — short and abstract, matching the style of the other three part titles (Part I "Foundations", Part II "Data munging", Part IV "The big picture") rather than describing technique literally. Also fixed a punctuation inconsistency found in the process: Part II used "Part II: Data munging" (colon) while the other three used " - " (hyphen); standardized all four on the hyphen.
- [ ] **Internal cross-reference links (`ch0NN.xhtml`) throughout the book don't match what pandoc actually generates.** (Found 2026-09-01, verifying the Part IV fix above with a real `make epub` build.) Front matter and several chapters (1, 3, 5, 8, 9, appendix-b) link to specific chapter files like `ch015.xhtml` assuming one file per chapter/part. In reality pandoc splits the EPUB at *every* heading — including sub-headings inside a chapter like "What is data munging?" — producing 32 files, not ~20, so most of these hand-numbered links likely land on the wrong section in a real EPUB reader (verified: e.g. the front matter's `ch015.xhtml` for "Chapter 10" is actually a sub-section of Chapter 2 in the real build). Pre-existing, book-wide, unrelated to the Part IV fix. Needs a decision: demote the sub-headings that shouldn't split their own file (so real filenames line up with the hand-written ones), or drop the hardcoded hrefs and just reference chapters by name without a link.
- [x] ~~Front matter is ahead of the chapter it describes.~~ **Resolved (2026-08-06):** Chapter 10 is now "Common Data Interchange Formats," covering XML, JSON, and YAML; front matter's TOC blurb updated to match.
- [x] ~~Check heading order in Chapter 5.~~ **Resolved (2026-09-08):** removed the "Converting the character set" H3 wrapper entirely (it never actually taught character-set conversion) and promoted Unicode to a top-level H2 in its own right, with its eight subsections promoted H4→H3 and a reinstated opening paragraph. "Data conversions" relocated to introduce just the two conversions still under it (line endings, number formats — wording updated from "three typical types" to "two"). Chapter's "What this chapter covers" bullets updated with a new "Working with Unicode text" line.

## Outdated code / modules, by chapter

- [x] ~~Ch5 — Unicode section says "Perl version 5.6 includes some support for Unicode"~~ **Resolved (2026-08-21):** rewrote around the real distinction — `use utf8` (source code) vs `Encode`/`:encoding(...)` (actual I/O) — built around a "patrol your borders" (decode in, encode out) worked example reading a file of accented artist names, a silly `$π` demo of what `use utf8` unlocks for identifiers, and `Text::Unidecode` for when ASCII-only really is the goal. Added `code-examples/c5/borders.pl`, `greek_pi.pl`, `unidecode.pl`, `artists.txt`. Forward-points to Ch10 for the JSON/YAML UTF-8 connection (see new item below).
- [ ] **Ch5 — Unicode section, round two.** Working through further polish one step at a time (started 2026-08-21). Done so far: EBCDIC removed entirely — the mainframe-conversion sentence in the character-set intro, and the whole `#### Converting between ASCII and EBCDIC` subsection/`Convert::EBCDIC` example (unmaintained on CPAN since 1998); also deleted `code-examples/c5/ascii2ebcdic.pl` and `ebcdic2ascii.pl`. Also done: audited every "ASCII" reference in the chapter and triaged each as genuinely-ASCII (left alone — FTP ASCII mode, `\cM`/`\cJ` control codes, ASCII codes 10/13 for line endings, Text::Unidecode's ASCII-safe output), recastable-as-Unicode (the "most textual data will be in ASCII" framing, both in the character-set intro and echoed in the chapter summary — now says most text is Unicode/UTF-8 by default), or vaguer "text" (the "ASCII text files" section heading and its intro, the "Reading a text file" chapter-overview bullet, "Reading the file" intro — none of that content is actually ASCII-specific). The "Converting line endings" intro's "an ASCII text file is..." line was the other spot flagged earlier for the same fix — done together with the character-set intro as planned. Also done: added a "Bytes versus Unicode strings" subsection (between the utf8-vs-Encode intro and "Patrol your borders") explaining that encodings only exist outside the program — bytes on disk vs. decoded characters in memory — and why UTF-8 is backwards-compatible with ASCII (0–127 map to the same single byte) while ISO-8859-X isn't, despite reusing the same 128–255 byte range for something else entirely; that byte-range collision is framed as the real source of mojibake. This satisfies the "Reading the file" intro's forward-pointer. Also done: added an "Other ways to represent Unicode characters" subsection (right after the `$π` demo) covering `\x{HHHH}`, `\N{U+HHHH}`, and `\N{CHARACTER NAME}` (confirmed via testing that named characters have worked without an explicit `use charnames` since Perl 5.16) — and made the point that none of these need `use utf8;`, since they're pure ASCII in the source, unlike typing the literal character. New file: `code-examples/c5/char_escapes.pl`. Also done: added "Unicode properties in regular expressions" (`\p{L}` vs `\w`'s over-eager matching of digits/underscore, `\p{Greek}` for script-restriction — something `\w`/`\d`/`\s` can't do at all, `\P{...}` negation to strip non-letters regardless of script; new file `code-examples/c5/properties.pl`) and "Combining characters and canonical representation" (precomposed vs. decomposed `é` — same visible text, different code point sequences, `eq` and `length` disagree; `Unicode::Normalize`'s `NFC` as the fix, confirmed core since Perl 5.7.3 via `corelist`; mentioned the classic macOS-decomposed-filenames gotcha; new file `code-examples/c5/normalize.pl`). Also done: added "Comparing and sorting Unicode text: fold case and collation" — `fc()` vs `lc()` for case-insensitive equality (German ß→SS case-folding as the motivating example, core since Perl 5.16 via `use feature 'fc'`) and `Unicode::Collate` for locale-aware sorting (default `sort` puts "über" after "zebra" by code point; `Unicode::Collate` puts it where a human would expect, core since Perl 5.8.0); new files `code-examples/c5/fold_case.pl` and `code-examples/c5/collate.pl`. Still to do: encoding detection/BOM handling — discussed 2026-08-22, decided BOMs are rare enough in practice to skip covering fully but leave the item open rather than closing it outright.
- [x] ~~EBCDIC also mentioned in Chapter 12~~ **Resolved (2026-09-14):** first pass reworded so Unicode read as the assumed default; on reflection, Dave dropped EBCDIC from the checklist entirely in favor of two encodings a reader is actually more likely to meet in a legacy export — ISO-8859-X and Shift-JIS. Line now reads "Is it plain ASCII, Unicode (most likely, these days), or something older like ISO-8859-X or Shift-JIS?" (Shift-JIS still shows up in Japanese government/bank CSV exports and older Windows software, even though ~99% of `.jp` websites have moved to UTF-8.) Prompted a related addition to Ch5 — see "New topics" below.
- [x] ~~Ch6 — Dates covered only via `Date::Calc` and `Date::Manip`.~~ **Resolved (2026-08-21):** replaced both sections with `Time::Piece` (core) and `DateTime` (CPAN), reworking the same three worked examples (date in x days, previous Saturday, first Monday of the year) using the versions from your dev.to post "Processing dates and times with Perl" (2021). Also updated "Choosing between date modules," "Further information," and the chapter summary to point at the new modules. Dropped the old Date::Manip-vs-localtime benchmark rather than inventing new numbers — worth redoing for real if we want a Time::Piece-vs-DateTime comparison. Appendix A still has full `Date::Calc`/`Date::Manip` reference sections — left alone per the note below, pending the Ch5 pass too.
- [x] ~~Ch7 — Binary data examples use `Image::Info` (cited at v0.04) and `MPEG::MP3Info`.~~ **Resolved (2026-09-01):** checked both against real CPAN data. `Image::Info` is actually fine — still actively maintained (v1.45, released Nov 2024), now covers far more formats than the book's "PNG, JPG, TIFF, GIF" (BMP, ICO, SVG, WEBP, AVIF, etc.) — just updated the stale "version 0.04" citation and added a pointer to `Image::ExifTool` for advanced EXIF work. `MPEG::MP3Info` (now `MP3::Info`) is genuinely stale — last release 2017, single maintainer, never got ID3v2 writing support. Replaced the worked example with `Audio::Scan` (last released June 2026), which also covers MP4/AAC, Ogg Vorbis, FLAC, WMA, WAV, AIFF, and more through one consistent API; noted that it's read-only (no tag writing, unlike the old module) and pointed to `MP3::Tag` for that case. New file `code-examples/c7/audio_scan.pl`, old `read_id3.pl` deleted. Confirmed working by Dave against a real MP3, which surfaced a follow-up: several ID3 tags (`APIC`, `COMM`, `USLT`) and `xing_toc` come back as array references — added a "Testing the Audio::Scan example" subsection with the real output plus an "Array-valued tags" subsection showing a small `format_value()` fix, and why it's not a real solution (a length-based heuristic, not per-tag understanding — new file `code-examples/c7/audio_scan2.pl`). Also fixed two more dated bits Dave spotted on a read-through: the "Binary data" intro assumed all prior data was ASCII (now generalized to text/encodings, pointing at Chapter 5 instead of re-teaching ASCII specifics), and the GIF/PNG intro dropped the Unisys patent scare story and a dead webtechniques.com link (the patent expired worldwide in 2004) in favor of an actual pedagogical reason to use PNG.
- [x] ~~Ch9 — Extended example ("weather forecasts")~~ **Resolved (2026-08-06):** added a closing note that the Yahoo! page is gone entirely, pointing to Chapter 10's live weather-API example. The HTML::TokeParser teaching content itself is left as-is — still a fine scraping lesson, just not something to build new code against.
- [x] ~~Ch11 — Building your own parsers.~~ **Done (2026-08-05).** Optional future polish: a one-line nod to `Marpa::R2` as another modern alternative.
- [x] ~~**Appendix B — Essential Perl** teaches the `-w` command-line flag but never mentions `use strict; use warnings;`, which is the standard opening for any Perl script today. Odd gap in a basics appendix.~~ **Resolved (2026-09-21):** turned into a full rewrite rather than a patch, once a read-through for this one fix turned up how much else in the appendix was stuck in 2001 (per Dave: "if we had 5-6,000 words to write an 'Essential Perl' guide today, what would it contain?"). Landed at ~4,950 words. New: a "The modern baseline" section (`use strict`/`use warnings`/`use v5.36;`, `use feature`/`use VERSION` bundles, `say`); a "Scope: `my`, `our`, and `local`" subsection (`our @ISA`/`our @EXPORT` is real code in Ch2 that was never explained anywhere); defined-or (`//`/`//=`) and the ternary operator in Operators (neither existed before); a brief OO pointer to Chapter 2 rather than re-teaching it. Flipped two things from old-style-first to modern-first: Subroutines now leads with signatures (`@_`/`shift` demoted to "you'll see this in older code"), and References now leads with postfix dereference (`->@*`/`->%*`) with the old `@{$ref}` block-deref style as the aside — matching how the book's own code (Ch2, Ch11) actually writes it. All examples switched from Lord-of-the-Rings names to the book's own CD-collection data, several ties back to real chapter code (Ch2's `count_cds_by_attr`, the `//=` customer-record pattern, Ch6's caching). Fixed a handful of outright bugs found along the way (`each %rings` had a stray extra paren, a `redo` example had two syntax errors, a hash literal was missing its closing quote) by simply not carrying them forward. Refreshed "Further reading": verified *Learning Perl* 8th edition is still current (no newer edition exists) and matches Chapter 1's own citation; dropped two out-of-print 1990s titles that didn't hold up next to it. Also absorbed the CPAN-install item below into the rewrite, since it's the same appendix. Every code example verified against a real Perl interpreter (this sandbox's 5.34, with the version-pragma line swapped, same workaround as the signatures retrofit) before being written up.
- [x] ~~Nowhere in the book explains how to actually install a module from CPAN~~ **Resolved (2026-09-21)**, folded into the Appendix B rewrite above: new "Installing modules from CPAN" section covers `cpanm`/`App::cpanminus` (including the real bootstrap one-liner, verified against current metacpan/cpanminus docs) as the practical default, the core `cpan` client as the thing that's always there but clunkier, and `cpanfile`/`cpanm --installdeps .` for multi-module projects. Added the agreed one-line pointer from Chapter 1's "Perl encourages code reuse" bullet.
- [x] **Ch2 — OO section is already solid.** Uses Moo, and accurately describes Perl's new core `class` feature as one to watch (confirmed still experimental as of Perl 5.42, the current stable release). No action needed.
- [x] ~~Ch8 — full modernity audit~~ **Resolved (2026-09-14):** diagrams were already redrawn, prose hadn't been checked. Found and fixed: two stale forward-references to `XML::Parser` (now `XML::LibXML`, matching Ch10's August rewrite); a stale forward-reference calling `Parse::RecDescent` "the most flexible tool for creating your own parsers in Perl" and pointing to Chapter 11 for it (now `Regexp::Grammars`, matching Ch11's rewrite; also dropped the long-dead `Parse::Yapp` from the "modules such as..." list); a real data bug where the chapter's two CD-file examples (plain vs. "expanded with tracks") disagreed with each other on three album years — reconciled against real release dates (Workers' Playtime 1988, Circus 1995, Earthling 1997, the last already established elsewhere in the book) rather than either table's original guess; and a likely leftover typo ("this will involve the use of parlance" → "...use of parsers", which also sets up the next heading, "An introduction to parsers," properly). Part III's own heading ("Simple data parsing") was flagged as misleading but deliberately left alone — see "Structural issues" above.
- [x] ~~Appendix A (Modules Reference) is now behind the chapters it documents~~ **Resolved (2026-09-21):** full rewrite. Removed `Date::Calc`, `Date::Manip`, and `XML::Parser` entirely — all three dropped from the narrative chapters (Ch6 replaced the first two with Time::Piece/DateTime back in August; Ch10 replaced the third with XML::LibXML) and, per Dave's call, superseded modules don't get a legacy entry here, they just go. Added new sections for `Time::Piece`, `DateTime`, `XML::LibXML`, `JSON::MaybeXS`, `YAML::PP`, `HTTP::Tiny`, `Regexp::Grammars`, and `Web::Query` (see new item below) — each one's method list grounded in what the actual narrative-chapter examples use, not a fresh unverified API dump. Also cleaned up `HTML::Parser`'s 2001-era "the ActiveState repository still only has a 2.x version" aside, and reordered every section to follow the book's own chapter sequence (DBI → Number::Format → Time::Piece/DateTime → LWP::Simple → HTML::Parser family → Web::Query → XML::LibXML → JSON::MaybeXS → YAML::PP → HTTP::Tiny → Regexp::Grammars) rather than its previous fairly arbitrary order.
- [x] **New: `Web::Query` added to Chapter 9 and Appendix A** (2026-09-21, not originally planned — came up while discussing the HTML::Parser-family Appendix A entries). Dave's own go-to for screen-scraping today; checked its CPAN currency first (last release Jan 2024, healthy bus factor, treated as stable-not-abandoned rather than stale). New "Web::Query: a modern alternative" section in `chapter09.md` after the Yahoo!-weather extended example, with a worked example (`code-examples/c9/webquery.html`/`webquery.pl`) scraping the book's own CD-collection data via CSS selectors — logic verified against a local stub matching Web::Query's documented API (module itself can't be installed in this sandbox, no CPAN access). Chapter 9 intro bullets and summary updated to match.
- [x] ~~Ch1, Ch2, and Ch12 — full modernity audits.~~ **Resolved (2026-09-21):** the three "very general" chapters, done in one afternoon per Dave's call. Ch1: mechanical fixes only — a missing word ("vendor-independent"), www.perl.com corrected to www.perl.org as the definitive Perl source, and a stray typo. Ch2: a stray backslash bug, smart quotes straightened, a missing comma, a broken Table 2.1 example row, three I/O-redirection bugs (stray quote, stray backslash, `%out_fh` → `$out_fh`), and "Kernigan" → "Kernighan" (×3, this chapter's Further Information section). Ch12 ("Looking Back and Ahead") turned out to be where the real staleness was hiding, in its "where to find Perl support" material: comp.lang.perl.misc (effectively dead since Google Groups dropped Usenet serving in Feb 2024) and *The Perl Journal* (folded decades ago, both its subscribe links dead — tpj.com and foo.be) were the two biggest offenders, mentioned as the primary/only channels in three separate spots. Replaced throughout with the current landscape: PerlMonks (kept, but reworded to acknowledge its age and recent uptime problems rather than oversell it), Perl Weekly, Planet Perl (the classic planet.perl.org domain now just redirects to perl.org — current successor is perl.theplanetarium.org, noted in the text), The Weekly Challenge, r/perl, Stack Overflow's `perl` tag (framed honestly as a searchable archive, not an active Q&A venue — SO's question volume has collapsed ~99% since 2014, confirmed via 2026 reporting), The Perl and Raku Conference (TPRC), and The Perl & Raku Foundation (both organizations' current official names, used in full despite Dave's reservations about the rebrand — anything else would read as bitterness). Also fixed: perl.com's listing corrected from "definitive source for all things Perl" (that's perl.org) to an honest "not official, but worth checking for articles and interviews"; a dead Mark-Jason Dominus article link turned out to still work fine, just needed "recent article" changed to "classic article" (it's from 2000); and a *Programming Perl* edition contradiction (one link pointed to the 4th edition while the text said "get the 3rd") resolved in favor of the 4th, which is current with no 5th edition published. Book-recommendation paragraph rewritten around the reality that the Perl book market has largely dried up: *Learning Perl* and *Programming Perl* are the two still worth buying new; *The Perl Cookbook* and (see next item) *Object Oriented Perl* downgraded to "worth a skim if you stumble across a copy, but don't go out of your way"; *Mastering Regular Expressions* dropped from the chapter entirely; Perl School added as the modern alternative — ebooks and, more recently, print-on-demand paperbacks, since 2017, because mainstream publishers had walked away from Perl as a topic (this book's second edition among them). Also, per Dave's decision on the long-open Damian Conway question: dropped *Object Oriented Perl* as a formal recommendation everywhere it appeared as one (Ch2 had two citations, Ch12 one), demoting it to the "dated but useful" mention in Ch12 in place of *Mastering Regular Expressions*. (Ch11's unrelated mention of Damian Conway as `Regexp::Grammars`' author is untouched — different person, different context.) Prompted, in the same session, a full postfix-dereference sweep across Chapters 2–11 and Appendix B — see the signatures item below for that detail, since it's really a follow-on from the same "what does this book's code look like today" conversation.

## Audit coverage — what hasn't been checked yet

Worth knowing when trusting the completion estimate above: the "no
modernization flags" chapters haven't all had the same level of
scrutiny. Ch1, Ch2, Ch3, Ch5, Ch6, Ch7, Ch8, Ch9, Ch10, Ch11, and Ch12
have all now been actively checked against current Perl/CPAN practice
(that's where the open items above come from) — Ch3's full modernity
audit completed 2026-09-08 (15 mechanical fixes, Benchmark section
restructured with current numbers, DBI section rewritten around
SQLite, new Testing section added); Ch8's completed 2026-09-14 (see
"Outdated code / modules" below); Ch1, Ch2, and Ch12's completed
2026-09-21 (see "Chapters 1, 2, and 12 audits" below). Ch4 got a full
read during the original July 2026 pass but not a dedicated audit —
scheduled for PLAN.md's Week 5, as the harder of the four. **Appendix
B was rewritten from scratch 2026-09-21** (see "Outdated code /
modules" below) so no longer belongs on this list either.

- [ ] **Audit every "available/bundled since Perl 5.X.Y" claim in the book for citing a real production release, not a development one** (flagged 2026-08-21). Perl's odd-numbered minor versions (5.7, 5.9, 5.11, 5.13, ...) have always been development tracks; the following even minor version is the first stable release, and that's the one our readers actually care about. Caught one instance already: Ch5 said Unicode::Normalize has been "bundled with Perl since 5.7.3" — true, but `corelist` confirms 5.8.0 (the first stable release after that) is the citation that should be in the book. Needs a pass over every chapter and appendix for the same pattern (Ch5/Ch6's own recent version claims — Time::Piece since 5.10, `Encode` since 5.8, Unicode identifiers since 5.10, `charnames` since 5.16 — are all already even/stable and look fine on a quick check, but haven't been re-verified specifically for this).

## New topics to consider for relevance

- [x] ~~Extended ASCII / legacy single-byte encodings in Chapter 5~~ — **Resolved (2026-09-14):** the existing "Bytes versus Unicode strings" section already explained Latin-1 (ISO-8859-1) specifically as a source of mojibake; added one brief paragraph right after it noting Latin-1 is one member of the wider ISO-8859-X family (2/5/7/... for Central European/Cyrillic/Greek/etc.), plus Windows' CP-1252 near-twin and why it's a classic real-world gotcha (smart quotes copied from Word decoding as "â€™"). Prompted by the Chapter 12 EBCDIC discussion above.
- [x] ~~JSON/YAML chapter to replace XML~~ — **done (2026-08-06)**, using `JSON::MaybeXS` and `YAML::PP` as planned. See sprint entries below for the full rewrite.
- [x] ~~DateTime / Time::Piece modernization of the dates section~~ **Resolved (2026-08-21)** — see Ch6 above.
- [ ] **REST/HTTP APIs as a data source**, more generally — the book only touches HTTP in passing right now. Ties together the JSON chapter and the dead weather-scraping example.
- [ ] **Modern HTTP client note** — `LWP::Simple` still works but lacks HTTPS/header/error-handling support that `HTTP::Tiny` (core) or `LWP::UserAgent` provide.
- [ ] **Unicode/encoding handling** as a more general topic — data munging today is rarely pure ASCII. Ch5 now covers the fundamentals (decode/process/encode, `use utf8` vs `Encode`); this item is about going further — real-world mangled-encoding troubleshooting, BOM handling, that kind of thing.
- [x] ~~Revisit Ch10's JSON/YAML encoding assumptions now that Ch5 lays the groundwork (2026-08-21).~~ **Resolved (2026-08-22):** new "Unicode in JSON and YAML" subsection (after "Working with YAML", before "Producing different document formats") covers `JSON::MaybeXS`'s two interfaces — `encode_json`/`decode_json` (utf8 mode: bytes in/out) vs the OO `JSON->new` form (chars in/out by default, bytes only with explicit `->utf8`) — and shows the resulting mojibake if you mix them up (verified against a local `JSON::MaybeXS` stub wrapping core `JSON::PP`, since the real module isn't installed in this sandbox). Also covers `YAML::PP`'s equivalent split: `load_file`/`dump_file` handle UTF-8 for you, `load_string`/`dump_string` expect/return already-decoded characters. Ties in Path::Tiny's `slurp_raw` vs `slurp_utf8` as the file-reading half of the same choice. New file `code-examples/c10/unicode_json.pl`. While in there, also found and fixed a latent bug in the existing "Producing different document formats" round-trip example: it printed `JSON->new->utf8->encode(...)` (bytes) and `YAML::PP->new->dump_string(...)` (decoded characters) to the same unlayered `STDOUT` back to back — harmless today because the CD data is all-ASCII, but would mangle the YAML output the moment someone added an accented artist name. Fixed by adding `binmode STDOUT, ':encoding(UTF-8)'` once and dropping the now-redundant `->utf8`, so both outputs go through the same layer. Updated `code-examples/c10/cds.pl` to match.

## More backlog items (from `data-munging-with-perl-2e-todos.json`, found 2026-08-05, since deleted — content captured here)

You'd already drafted an 18-item modernization backlog in this file (with `gh`-based scripts to turn it into GitHub Issues). Most overlaps with the above; these are the ones that don't:

- [x] ~~`Path::Tiny` as a cleaner, safer alternative to manual `opendir`/`glob`/`unlink`/`open` for file and directory ops.~~ **Resolved (2026-08-22):** added a new "Path::Tiny" section in Ch3 (after "Command line scripts") — `path()`, `lines`/`slurp`/`spew`, `child` for portable path-joining, and `exists`/`touch`/`remove`/`children` as a group, all shown by rewriting the chapter's existing CD-file parsing example. Also added a follow-up in Ch5's "Patrol your borders" section showing `lines_utf8`/`slurp_utf8`/`spew_utf8` as a one-call alternative to `open ... '<:encoding(UTF-8)'`, cross-referencing back to Ch3. New files `code-examples/c3/path_tiny.pl`, `code-examples/c5/path_tiny_borders.pl` — logic verified against a local stub matching Path::Tiny's documented API (module itself can't be installed in this sandbox, no CPAN access).
- [x] ~~`CHI` or `Cache::Memory`~~ **Decided against (2026-09-21):** staying out of the book, ahead of writing Appendix A so its module list is final. No new section, no Appendix A entry.
- [x] ~~New section on testing~~ **Resolved (2026-09-08):** added a "Testing" section to Chapter 3, between Data::Dumper and Benchmarking. Covers `Test::More` first (a worked example trimming whitespace from a string, using `is`/`ok`/`like`/`done_testing`, with real TAP output verified against a live run), then a shorter "Test2" subsection explaining that Test::More is now itself built on Test2, and that `Test2::V0` is the modern recommended entry point—covering roughly the same ground as the original plan's `Test2::V0`-only idea, but leading with Test::More since it's still what most existing Perl code and tutorials use. Chapter intro bullets and summary updated to match.
- [x] ~~`Benchmark::Timer` or `Time::HiRes`~~ **Decided against (2026-09-21):** staying out of the book, same call as `CHI`/`Cache::Memory` above — existing `Benchmark.pm` coverage stands as-is, no Appendix A entry needed.
- [ ] **Audit all `use` statements for core vs. CPAN** — annotate which modules ship with Perl and which need installing, and make sure things like `say`, `state`, and signatures are explicitly enabled where used.
- [x] ~~Replace `@_`/`shift`-based argument unpacking with subroutine signatures throughout the book.~~ **Resolved (2026-09-21):** groundwork (the Chapter 3 "Feature pragmas" section) landed 2026-09-14; the conversion pass itself is now done. Converted 16 subs across 10 files in `code-examples/` (c2, c3, c4, c6, c7, c9) plus the matching inline copies in chapter02.md, chapter03.md (the `trim` example only — the Chapter 3 before/after `greet` illustration was deliberately left as `my ($name) = @_` since that's the point being taught), chapter04.md, chapter06.md, chapter07.md, and chapter09.md. Also caught and fixed one the retrofit sweep turned up that wasn't in the original scope: the new Web::Query example's `->each(sub { my $i = shift; ... })` callback, converted to `->each(sub ($i) { ... })`, in both chapter09.md and `code-examples/c9/webquery.pl`.

  Left alone, deliberately: sort-comparator subs (`namesort`, `orcish`, `numerically`, etc.) that read the package globals `$a`/`$b` rather than taking arguments — signatures don't apply; the Chapter 3 `greet` before/after teaching example; Appendix B's entire "Subroutines" primer section (`gollum`, `example`, `element` ×2, `my_sub`), which is specifically teaching how `@_` and argument-flattening work, not just using it; every top-level `my $x = shift;` that's unpacking `@ARGV`, not a sub's arguments (there are quite a few of these, in both code-examples and chapters — left untouched since they're not what the retrofit was about). One slurpy-array case came up for real: `code-examples/c9/linkextor2.pl`'s `check` callback (variable arity, called by `HTML::LinkExtor`) converted to `sub check(@args)` rather than a fixed positional list.

  Verified: `perl -c` against a copy of every changed file with the version pragma swapped for `use v5.34; use feature 'signatures';` (this sandbox only has Perl 5.34) plus small stub modules for the two CPAN modules not installed here (`Text::CSV`, `Audio::Scan` — the usual no-CPAN-sandbox workaround). Ran `testing.pl`, `sprintf_ex.pl`, `cd_full.pl`, and `translate.pl` for real and diffed output against the pre-conversion originals — byte-identical in every case. Also ran the `html_parser.pl`/`linkextor2.pl` callback patterns against the real (installed) `HTML::Parser`/`HTML::LinkExtor` modules directly, and the `sub ($i) { ... }` `each`-callback pattern against a small fake iterator, to specifically exercise the two less-obvious conversions (positional 3-arg parser callback, slurpy-array callback).

  Found one orphaned file while sweeping for `@_`/`shift`: `code-examples/c2/Customer.pm` uses the newer `class`/`method` feature (not `sub`), and has `my $self = shift;` inside two `method` blocks — which is redundant at best (the `method` keyword already binds `$self` for you; `@_` inside a method doesn't include the invocant) and doesn't match anything actually shown in chapter02.md's prose, which uses a Moo-based example instead. Left as-is for now, per Dave (2026-09-21) — not a priority this week, revisit later.

  Follow-up (2026-09-21), per Dave: since the conversion meant Chapter 2's code already uses signatures before Chapter 3 gets around to explaining them, added a short aside to Chapter 1 (right after the existing "bareword filehandles" aside, matching its style) flagging up front that every subroutine from the next chapter on uses a signature, with a forward pointer to Chapter 3 for the full story. Reworded Chapter 3's "From this point on..." sentence to acknowledge Chapter 2's examples explicitly, since it's no longer accurate that the convention only starts there. Also added a closing note to Appendix B's "Subroutines" section (the `@_`/argument-passing primer, deliberately left untouched during the conversion pass) pointing out that the book actually uses signatures throughout, with a one-line example and a pointer to Chapter 3.

  Second follow-up (2026-09-21), prompted by Dave spotting `push @ {$year{$years}}, $rec;` in Chapter 2 and asking whether it was a good spot for postfix dereference: turned into a full sweep of old-style dereferencing (`@{...}`/`%{...}` block-deref and `@$ref`/`%$ref` sigil-deref) across Chapters 2, 4–11, and Appendix B, converting to postfix deref (`->@*`/`->%*`) wherever the thing being dereferenced is a compound expression — a hash/array element, a method call, a chained lookup — rather than a bare scalar variable. For a bare scalar (`@$foo`), the two styles read about equally well, so those were deliberately left alone; Appendix B's References section gained a new passage explaining exactly this distinction, with `push @{ $cds_by_year{$cd->{year}} }, $cd` vs `push $cds_by_year{$cd->{year}}->@*, $cd` as the worked comparison. Every converted block verified against a real Perl interpreter (stubbing CPAN modules not available in the sandbox where needed), output/behaviour compared against the pre-conversion original. Also caught and fixed two spots left inconsistent by an earlier pass — `code-examples/c2/cd_full.pl`'s `$counts->%*` and `code-examples/c11/ini.pl`'s `$assignments->@*`, both bare-scalar cases that should have matched the new rule — bringing the whole book in line with the same convention.
- [ ] **Closing appendix: glossary of modern CPAN modules**, grouped by purpose (logging, JSON, web, OO, date/time, etc.) — natural companion to the existing Appendix A module reference.
- [x] ~~Decide XML's fate precisely.~~ **Decided (2026-08-05), done (2026-08-06):** the chapter is JSON/YAML/XML, not a straight swap — XML stays, trimmed down and modernized (`XML::LibXML` rather than `XML::Parser`/`XML::DOM`), because "some poor souls still use it." Retitled **Chapter 10: Common Data Interchange Formats**.

## Artwork

- [x] ~~Redraw the figures.~~ **Done (2026-08-26):** all 20 previously-referenced 1st-edition-era diagrams redrawn as hand-authored SVGs in a new flat style (square corners for array boxes, rounded corners for hash boxes, teal for process-flow arrows, violet for reference/pointer arrows — keeping the original color distinction). Covers Ch1 (`1-1`), Ch2 (`2-1` through `2-5`), Ch3 (`3-1` through `3-4`), Ch8 (`8-1` through `8-4`), and Ch11 (`11-1`, `11-2`), all reviewed and approved by Dave through several rounds of feedback. `11-3-item-array.png` (the old Parse::RecDescent `@item` diagram) stays untouched/unreferenced for now — Dave's call, revisit later.
- [ ] **Find a home for `foreword-diagram-key.svg`.** (2026-08-26) This was the source used to establish the array/hash corner convention above, but it turns out it's never actually been wired into the book — confirmed via grep, only mentioned in this TODO file. Redrawn in the new style with a small caption under each of its three panels (array, hash, reference). Dave confirmed the book should use it — needs a spot, likely early in Chapter 1 or 2 near the first data-structure figure, as a "how to read these diagrams" note.
- [ ] **Verify SVG arrowheads render correctly outside this sandbox.** (2026-08-26) Discovered while building `8-2-simple-grammar.svg` that this sandbox's SVG-to-PNG preview (ImageMagick without `rsvg-convert`) silently fails to draw `<marker>`-based arrowheads — every arrow in every redrawn diagram (`1-1` through `8-1`, `8-3`, `8-4`) has been previewed as a plain line with no visible tip, even though the SVG markup is valid and standards-compliant. Confirmed by zooming into already-approved files (e.g. `3-2`'s reference arrows, 8-2's Terminal callout) — no triangle in the rendered PNG. Real renderers (browsers, EPUB readers, WeasyPrint for the PDF build) should draw these correctly since it's ordinary `marker-end` syntax, same category as the earlier gradient/dash-array limitation hit on `2-5`. `8-2-simple-grammar.svg` was rebuilt to use inline `<polygon>` triangles instead (so it previews correctly here); the other files were left using `<marker>` per Dave's call — **please spot-check at least one file (e.g. open `3-1-the-unsorted-array-of-cd-hashes.svg` in a browser) before the release build**, and let me know if arrowheads are missing there too, in which case all files should be retrofitted to inline triangles.
- [ ] **Scan chapters 4–7 for new diagram opportunities.** (2026-08-26) Noticed while redrawing the existing figures that chapters 4–7 currently have none at all, which stands out next to how diagram-heavy chapters 1–3 are. Worth a pass to find spots where a data-structure or flow diagram would help — candidates to check: Ch4's regex matching/substitution flow, Ch6's date arithmetic examples, Ch7's binary/packed-data layouts. Not yet started.
- [ ] **Refresh the CD collection example data.** (2026-08-26) The album years in the running CD example (Hunky Dory 1971, Earthling 1997, Workers' Playtime 1987, etc.) read as current in a book written in 2001 but look distinctly dated now — worth picking more recent releases for at least the diagrams and prose that show explicit years, so the examples don't quietly date the whole book. Handy side-effect of the SVG redraw: once the data changes, updating a diagram to match is a quick text edit instead of redrawing an image from scratch.
- [ ] **Three images now orphaned** (found via a `make epub` image-count check, 2026-08-06): `11-3-item-array.png`, `10-1-output-from-xml-parser-tree-style.png` (both above), plus `foreword-diagram-key.png`, which isn't referenced anywhere in the current text at all — worth checking whether the foreword ever pointed to it, or whether it can just be dropped.
- [x] ~~New diagram needed for Ch5's "Bytes versus Unicode strings"~~ **Resolved (2026-08-22):** built as a hand-authored SVG (`images/5-1-ascii-vs-utf8-encoding.svg`), not an AI-generated image — the content is exact technical data (hex byte values, code points), which text-to-image models can't reliably render as legible, correct text. Four-column layout (Character → Code Point → ASCII Bytes → UTF-8 Bytes), one row for "A" (identical single byte, 0x41, in both encodings) and one for "é" (ASCII can't represent it; UTF-8 needs two bytes, 0xC3 0xA9), with the code point shown as the middle step as requested. Inserted into Ch5 right after the ASCII/UTF-8/mojibake prose it illustrates. Verified end-to-end: rendered correctly via `librsvg`/ImageMagick, and confirmed pandoc embeds the SVG cleanly into the EPUB (`media/file0.svg` in a test build) — the PDF path (WeasyPrint) still needs Dave's own build to confirm, per the usual sandbox limitation.

## 2026-08-06 — Chapter 10 capstone example rewritten

- [x] **"Producing different document formats" replaced.** The old ~570-line section (258-line `XML::Parser`-based dispatch script converting a README into POD/HTML/text) is gone. New "one dataset, three formats" example: parses the book's 6-CD collection from `cds.xml` via `XML::LibXML` + XPath, emits JSON (`JSON::MaybeXS`) and YAML (`YAML::PP`) from the same in-memory structure — ~17 lines of code total, plus a short "Going back the other way" subsection covering XML round-tripping in prose only (no code, per your request). Files promoted to `code-examples/c10/cds.xml` and `cds.pl`; the old `readme.xml`/`transform.pl` example files are now orphaned and were deleted.
- [x] ~~Untested~~ — **confirmed working** by Dave, pushed.
- [ ] **Still outstanding for the full Ch10 rewrite:** this is one example, not the whole chapter restructure. Still need: dedicated JSON section, dedicated YAML section, and a decision on how much of the remaining `XML::Parser`/`XML::DOM`/`XML::RSS` content gets trimmed down to `XML::LibXML` vs. cut outright. Chapter title/intro ("What this chapter covers") not yet updated to reflect JSON/YAML.

## 2026-08-06 — XML::Parser / XML::DOM replaced with XML::LibXML

- [x] **"Parsing XML with XML::Parser" (5 styles: Stream/Debug/Subs/Tree/Objects) and "XML::DOM" sections replaced.** ~760 lines covering two legacy modules cut down to one "Parsing XML with XML::LibXML" section (~125 lines): XPath-based parsing (`findvalue`/`findnodes`) using the same `weather.xml` example as before, a short generic tree-walker, the same `eval`-based error handling, a note on `XML::LibXML::Reader` for streaming huge files, and a closing paragraph naming `XML::Parser`/`XML::DOM` for readers who meet them in old code — no worked examples for either. Chapter's "What this chapter covers" bullets and Summary updated to match — they still described the old structure. Chapter now ~575 lines, down from ~1215; will be padded back out once JSON/YAML sections exist.
- [ ] **Untested** — same caveat as everything else this sprint: no CPAN access in this sandbox. Please run the `weather.xml` examples before publishing.
- [ ] **Orphaned image:** `images/10-1-output-from-xml-parser-tree-style.png` (the Tree-style diagram) is no longer referenced anywhere — same situation as `11-3-item-array.png`, candidate for the "redraw the figures" pass.

## 2026-08-06 — Chapter 10 renamed, XML markup style, JSON/YAML sections written

- [x] **Chapter 10 renamed "Common Data Interchange Formats."** Intro rewritten: HTML's shortcomings (kept), then XML/JSON/YAML each explained in turn using the same weather-forecast example rendered in all three formats, closing with a "reach for JSON talking to an API, YAML when a human edits the file, expect XML in older/document-centric systems" summary. XML's DTD/valid-vs-well-formed material trimmed to two paragraphs. Front matter TOC blurb updated to match.
- [x] **All-caps XML tags lowercased.** You flagged `<FORECAST>`/`<OUTLOOK>`/`<TEMPERATURE>` etc. as hard to read — swept to lowercase across `chapter10.md`, `weather.xml`, and `weather_xpath.pl`. Attribute values (`MAX`/`MIN`/`C`) left as-is; nothing else in the repo had the same problem.
- [x] **New "Working with JSON" section.** `JSON::MaybeXS` intro, then a live example calling the [Open-Meteo](https://open-meteo.com/) weather API (free, no key) for London's current conditions and today's high/low — this is the real, working replacement for Chapter 9's dead Yahoo! scraper, cross-linked both ways. Verified the actual JSON response shape against Open-Meteo's own docs rather than guessing.
- [x] **New "Working with YAML" section.** `YAML::PP` reading a small hand-written `cities.yaml` (name/lat/long per city), looping over it to call the same API per city — YAML as human-edited config, JSON as wire format, same data shape either way once parsed.
- [x] **Chapter 9 updated** with a closing note that the Yahoo! page is now gone entirely, pointing to Chapter 10. Deleted the two superseded draft scripts (`new-code/weather_scrape_2025`, `new-code/c09/weather`).
- [x] **Ch10 "What this chapter covers" and Summary** updated for JSON/YAML; "Further information" corrected — `HTTP::Tiny` is core Perl (5.14+), unlike the other modules in the chapter.
- [ ] **Untested, and this one makes real HTTP calls** — `code-examples/c10/weather_api.pl` and `cities_weather.pl` hit the live Open-Meteo API. Worth running before publishing, both to confirm the code works and that the response shape hasn't changed.
- [ ] **XML::RSS not touched.** Still uses `XML::Parser` internally (that's fine, it's a stable dependency, not something the chapter teaches directly) — worth a quick look now everything else in the chapter is modernized, but not urgent.

## Next session

**Priority (decided 2026-08-26):** Dave wants to get to a position where every chapter itself — not just appendices/artwork — has been checked and modernized. Ch3 ("Useful Perl idioms"), Ch7 ("Binary data" — need to confirm exact title/scope), and Ch8 ("Complex data formats," Part III opener) are next, since none of the three has had a real modernity audit yet: Ch3 and Ch8 only got the July PDF-sync diff, and Ch7's `Image::Info`/`MPEG::MP3Info` currency was flagged but never actually checked. Suggested order: Ch7 first (narrowest, already has a known concrete question — are those two modules still reasonable to teach), then Ch3 and Ch8 (broader "full modernity read" like the original Ch5/Ch6/Ch9/Ch10/Ch11 passes).

- [x] ~~Audit Chapter 7 (binary data) for outdated modules/techniques, starting with `Image::Info`/`MPEG::MP3Info`.~~ **Done (2026-09-01)** — see "Outdated code / modules" above.
- [x] ~~Full modernity audit of Chapter 3 (useful Perl idioms)~~ **Done (2026-09-08)** — 15 mechanical fixes, Benchmark section restructured (current numbers first, 2001 numbers as historical aside), DBI section rewritten around SQLite with a DBD-module comparison table, new Testing section (Test::More + Test2::V0) added. Chapter 5's heading order (see "Structural issues" above) tidied up in the same week.
- [x] ~~Full modernity audit of Chapter 8 (complex data formats / Part III opener)~~ **Done (2026-09-14)** — see "Outdated code / modules" above for detail. Quick keep-or-trim decision on Chapter 12's EBCDIC mention also made the same day (kept, reworded).
- Appendix A (module reference) rewrite is next — see Artwork/Outdated-code sections above for what it's missing.
- Review the copyright wording in `front-matter.md`.
- Run and check the new Ch10 weather examples (`weather_xpath.pl`, `weather_walk.pl`, `weather_api.pl`, `cities_weather.pl`, `cds.pl`) and the Ch11 `cds.pl` — see "Untested" notes above and in prior sprints.
- Automation: GitHub Actions workflow to build on push, once the current Makefile has proven itself over a release or two (deferred for now).
